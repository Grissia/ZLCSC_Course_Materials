service chal
{
    disable = no
    type = UNLISTED
    socket_type = stream
    protocol = tcp
    server = /bin/sh
    server_args = /home/chal/run.sh
    user = chal
    port = 5000
    flags = REUSE
    bind = 0.0.0.0
    wait = no
}
